{
  "name": "e-youthpreneur",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs",
    "kullanici": "node --env-file-if-exists=.env scripts/kullanici.mjs"
  },
  "dependencies": { "pg": "^8.23.0" }
}
